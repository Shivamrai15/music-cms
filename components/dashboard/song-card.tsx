import { Song } from "@prisma/client"

interface SongCardProps {
    song : Song
}

export const SongCard = ({
    song
}: SongCardProps) => {
    return (
        <div>
        
        </div>
    )
}
